
fetch('${URL}', {
      method: 'POST'
    , headers: new Headers({
        'Content-Type': 'application/x-www-form-urlencoded'
      })
    , body:
            'text='  + encodeURIComponent(notification.message) + 
            '&desp=' + encodeURIComponent(notification.url)
    })
